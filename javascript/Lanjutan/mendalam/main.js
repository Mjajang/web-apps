/* BigInt */
/* 
  Example:
  let bigInt = 1234567890123456789012345678901234567890n;
  console.log(bigInt);
  let bigInt2 = BigInt(1234567890123456789012345678901234567890);
  console.log(bigInt2);
*/

/* Symbol */
/* 
  Example:
  let id = Symbol('id');
  console.log(id);
  let id2 = Symbol('id');
  console.log(id2);
  console.log(id == id2);
*/

/* Mutable Immutable */
/* 
  trivia:
  - cummonly data type primitive is immutable and object/reference is mutable
  - immutable: data yang tidak bisa diubah
  - mutable: data yang bisa diubah
  opinion:
  - immutable is replaceable
  - mutable is modifiable
*/

/* Garbage Collection */
/* 
  trivia:
  - garbage collection is automatic memory management
  - garbage collector is a part of javascript engine
*/

/* Memory Leak */
/* 
  trivia:
  - memory leak is a situation where a program uses memory for no good reason
  - memory leak is a part of garbage collection
*/
